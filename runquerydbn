#     sprintf(name,"%s",argv[1]);
#     sprintf(rsqlstmt,"%s",argv[2]);
#     printpath=atoi(argv[3]);
#     printheader=atoi(argv[4]);
#     dirsummary=atoi(argv[5]);
#     numdbs=atoi(argv[6]);
#     sprintf(tabnam,"%s",argv[7]);
echo "query on out dbs create union view and query that view on entries subset"
./querydbn outdb "select * from vsument;" 0 1 1 2 sument
echo "query on out dbs create union view and query that view on summaries subset sum/orderby"
./querydbn outdb "select type,uid,sum(totfiles) from vsument group by type;" 0 1 1 2 sument
echo "query on out dbs create union view and query that view on summaries subset"
./querydbn outdb "select * from vsumsummary;" 0 1 1 2 sumsummary
