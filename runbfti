#
#     sprintf(in.name,"%s",v[1]);
#     in.printdir=atoi(v[2]);
#     in.maxthreads = atoi(v[3]);
#     in.writetsum = atoi(v[4]);
echo "tree index to screen"
./bfti testdirdup/testdir 1 2 0
echo "tree index to db"
./bfti testdirdup/testdir 1 2 1
./querydb testdirdup/testdir "select * from treesummary;" 1 1 0
